<UserControl x:Class="Thetacat.UI.Controls.MediaItemsListControl.MediaItemsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Thetacat.UI.Controls.MediaItemsListControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView Name="mediaItemsListView" ItemsSource="{Binding Path=Items, RelativeSource={RelativeSource AncestorType=UserControl}}" MaxHeight="300">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Path=ID}" />
                <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path=Path }" />
                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Path=MimeType }" />
                <GridViewColumn Header="Details" DisplayMemberBinding="{Binding Path=Details }" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
