<Window x:Class="Thetacat.UI.Explorer.ApplyMetatag"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Thetacat.UI.Explorer"
        xmlns:metatags="clr-namespace:Thetacat.Metatags"
        xmlns:controls="clr-namespace:Thetacat.Controls"
        mc:Ignorable="d"
        Title="ApplyMetatag" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">Select one or more tags to apply:</TextBlock>
        <controls:MetatagTreeView x:Name="Metatags" Grid.Row="1"/>
        <Grid HorizontalAlignment="Right" Margin="0 0 0 5" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64" />
                <ColumnDefinition Width="64" />
            </Grid.ColumnDefinitions>
            <Button Content="Cancel" IsCancel="True" Grid.Column="0" Height="24" Margin="0 0 5 0" />
            <Button Content="Save" IsDefault="True" Click="DoSave" Grid.Column="1" Height="24" Margin="5 0 0 0" />
        </Grid>
    </Grid>
</Window>
